{
  "name": "my-app",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "deploy": "appwrite functions createDeployment --functionId predict-bun --code . --activate true"
  },
  "dependencies": {
    "@gravlabs/appwrite-hono-adapter-bun": "^0.5.0",
    "hono": "^4.5.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}